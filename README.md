# AzureCorp-Simulator
Progetto di Tesi in Python per Unipegaso

Per poter avviare il progetto è necessario aver installato le librerie Matplotlib e Numpy
Con l'utilizzo del comando "pip install matplotlib numpy"
